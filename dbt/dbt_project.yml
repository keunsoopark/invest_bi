name: 'invest_bi'
version: '1.0.0'

profile: 'invest_bi'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets: 
  - "target"
  - "dbt_packages"

models:
  invest_bi:
    +materialized: view
    stg:

    int:

    mart:
      +materialized: table
