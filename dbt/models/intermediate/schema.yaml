version: 2

models:
  - name: int_fx_filled
    description: |
      Intermediate model that fills missing fx dates using last-known values,
      materialized as incremental for efficiency.
    columns:
      - name: date
        data_type: date
        description: ""

      - name: usdnok
        data_type: float64
        description: ""

      - name: nokkrw
        data_type: float64
        description: ""

  - name: int_transactions_in_nok
    description: |
      Intermediate model that converts transaction amounts to NOK using the FX rates at the transaction date.
      Need to be changed to incremental model
